package com.example.diuride.Listener;

import com.example.diuride.models.RiderNotificationModel;
import com.example.diuride.models.RlistModel;

public interface NotificationListener {
    void onRiderNotificationclick(RiderNotificationModel RnotificationModel);
}
